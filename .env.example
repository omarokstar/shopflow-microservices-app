# Copy to .env for running services individually with `nest start` (not Docker).
# When using docker-compose, these are set directly in docker-compose.yml instead.

JWT_SECRET=dev-secret-change-me

GATEWAY_PORT=3000

AUTH_SERVICE_HOST=localhost
AUTH_SERVICE_PORT=3001
AUTH_DB_HOST=localhost
AUTH_DB_PORT=5433
AUTH_DB_USER=postgres
AUTH_DB_PASSWORD=postgres
AUTH_DB_NAME=authdb

PRODUCT_SERVICE_HOST=localhost
PRODUCT_SERVICE_PORT=3002
PRODUCT_DB_HOST=localhost
PRODUCT_DB_PORT=5434
PRODUCT_DB_USER=postgres
PRODUCT_DB_PASSWORD=postgres
PRODUCT_DB_NAME=productdb

ORDER_SERVICE_HOST=localhost
ORDER_SERVICE_PORT=3003
ORDER_DB_HOST=localhost
ORDER_DB_PORT=5435
ORDER_DB_USER=postgres
ORDER_DB_PASSWORD=postgres
ORDER_DB_NAME=orderdb

RABBITMQ_URL=amqp://localhost:5672


SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=no-reply@shopflow.com


